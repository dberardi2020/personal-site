---
// Default skin is set in src/config/skins.ts (DEFAULT_SKIN).
// Update that value AND the import below together when changing the default.
import DefaultSkin from '../skins/Matrix.astro';
---
<DefaultSkin />
